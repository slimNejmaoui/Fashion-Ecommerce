import { FiltreCategoriePipe } from './filtre-categorie.pipe';

describe('FiltreCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
